#include <stdio.h>

/**
  *main - prints all single digit numbers of base 10 starting from 0.
  *Return: 0 means success
  */

int main(void)
{
	for (int a = 48 ; a <= 57 ; a++)
	{
	putchar(a);
	}
	return (0);
}
